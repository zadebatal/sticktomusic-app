/**
 * Platform utilities — re-exports from centralized config.
 * Kept for backward compatibility with existing imports.
 */
export { PLATFORM_META, ALL_PLATFORMS, getProfileUrl, formatFollowers } from '../config/platforms';
